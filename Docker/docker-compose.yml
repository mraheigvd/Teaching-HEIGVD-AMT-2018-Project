db:
  image: mysql:latest
  environment:
    MYSQL_ROOT_PASSWORD: root
    MYSQL_DATABASE: amt_project
  ports:
    - "3306:3306"
  volumes:
    - ./db:/docker-entrypoint-initdb.d

backend:
  image: tomcat
  ports:
    - "9876:8080"
  volumes:
  - ./Gamification-WP1.war:/usr/local/tomcat/webapps/Gamification-WP1.war
  links:
    - db